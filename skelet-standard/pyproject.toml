[project]
name = "skelet-standard"
version = "0.1.0"
description = "Standard skill/prompt pack for skelet (patch, ...)"
requires-python = ">=3.11"
dependencies = [
    "skelet",
]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
